import React from 'react'

const styles = {
  Header: {
    textAlign: 'center'
  }
}

const Header = (props) => (
  <header style={styles.Header}>
    <h1>Recipe Box</h1>
  </header>
)

export default Header
